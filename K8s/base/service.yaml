apiVersion: v1
kind: Service
metadata:
  name: cleanarchitecture-auth
  labels:
    app: cleanarchitecture-auth
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: cleanarchitecture-auth