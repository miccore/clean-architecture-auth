images:
  - name: cleanarchitecture-auth
kind: Kustomization
resources:
- ../../base
configMapGenerator:
  - literals:
    - ASPNETCORE_ENVIRONMENT="Production"
    name: cleanarchitecture-auth-map-config